/**
 * Main JS file for Casper behaviours
 */

 /* globals jQuery, document */
 (function ($, undefined) {
  "use strict";

  var $document = $(document);

  // hacky fix to make images work with pure grid
  $('img').addClass('pure-img centered');

})(jQuery);
